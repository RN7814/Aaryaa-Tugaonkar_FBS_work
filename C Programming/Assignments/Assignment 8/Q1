//6. Accept array and print only prime numbers of array. 
#include<stdio.h>
void prime(int *);
void main()
{
    int num[10];
    for(int i=0; i<10 ;i++)
    {
    printf("Enter a NO :");
    scanf("%d",num[i]);
    }
    prime(&num[0]);

}

void num(int* ptr)
{
    int i=2,j=0;
    while(j<10)
    {
        if(*ptr % i!=0)
        printf("%d-",*ptr);
        ptr++;
    }
}