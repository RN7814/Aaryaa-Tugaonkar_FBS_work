//4. Find odd and even among the numbers. 

#include<stdio.h>

void main()
{
    int no[100];
    for(int i=0; i<100; i++)
    {
        if(i%2==0)
        no[i]=i;
        else
        no[i]=i;

    }
    for( int a=0; a<100 ; a++)
    printf("%d  ",no[a]);
}
